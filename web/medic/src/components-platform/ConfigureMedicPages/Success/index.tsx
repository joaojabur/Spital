import React from "react";
import { PagesProps } from "../../../platform-pages/ConfigureMedic";

const Success = ({ previousPage, nextPage }: PagesProps) => {
  return <div className="landing"></div>;
};

export default Success;
